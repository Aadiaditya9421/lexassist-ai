# ── LexAssist AI — Python dependencies ───────────────────────────────────────
# Install: pip install -r requirements.txt
# Verified: Python 3.11+ | April 2026
#
# Minimum versions are specified (>=) so the project stays compatible with
# minor upstream updates. The exact versions tested are noted in comments.

# ── Core agentic framework ────────────────────────────────────────────────────
langgraph>=0.2.0                # tested: 1.1.6   — StateGraph, MemorySaver
langchain>=0.3.0                # tested: 1.2.15  — Document, splitters, schema
langchain-openai>=0.2.0         # tested: 1.1.14  — ChatOpenAI wrapper
langchain-community>=0.3.0      # tested: 0.4.1   — Chroma, HuggingFaceEmbeddings
langchain-core>=0.3.0           # tested: needed for Document class
langchain-text-splitters>=0.3.0 # tested: needed for RecursiveCharacterTextSplitter

# ── Vector store & embeddings ─────────────────────────────────────────────────
chromadb>=0.5.0                 # tested: 1.5.8   — persistent vector store
sentence-transformers>=3.0.0    # tested: 5.4.1   — all-MiniLM-L6-v2 embeddings

# ── Frontend ──────────────────────────────────────────────────────────────────
streamlit>=1.40.0               # tested: 1.56.0  — chat UI + metrics display

# ── Evaluation ────────────────────────────────────────────────────────────────
ragas>=0.2.0                    # tested: 0.4.3   — faithfulness scoring metrics

# ── Utilities ─────────────────────────────────────────────────────────────────
pyyaml>=6.0                     # tested: 6.0.3   — YAML frontmatter parsing
python-dotenv>=1.0.0            # tested: 1.2.2   — .env loading for API keys

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0.0                   # tested: 9.0.3   — test runner
pytest-cov>=5.0.0               # tested: 7.1.0   — coverage reporting
